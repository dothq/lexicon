This folder is intentionally left blank for model downloading.

Run
```
python3 models.py "list_models"
```
to list the available models, otherwise run
```
python3 setup.py
```
to install all the available models.