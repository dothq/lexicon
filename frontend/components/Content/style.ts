import styled from "@emotion/styled";

export const Content = styled.main`
    max-width: 1200px;
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    gap: 2.3rem;
    padding: 0 1.6rem;
`;